name: mlops ci

on: [push]

jobs: 
  train-model:
    runs-on: ubuntu-latest

    steps:
    - name: train ci-cd
      run: |
        echo "train ci-cd setup successful"

  test-model:
    runs-on: ubuntu-latest

    steps:
    - name: test ci-cd
      run: |
        echo "test ci-cd setup successful"
